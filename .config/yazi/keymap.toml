# [[mgr.prepend_keymap]]
# on   = "i"
# run  = "open --hovered --opener=showinfo"
# desc = "Show media info"

[mgr]
prepend_keymap = [
  { on = ["R","b"], run = "plugin recycle-bin", desc = "Open Recycle Bin menu" },
  { on = [".","s"], run = "plugin what-size -- '--clipboard'", desc = "Calc size of sel/cwd + paste to clipboard" },
]

